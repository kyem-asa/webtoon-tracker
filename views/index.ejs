<!DOCTYPE html>
<html lang="en" data-theme="cupcake">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>webtoon tracker</title>

    <link href="css/output.css" rel="stylesheet">

</head>
<body>
    <h1 class="text-3xl font-bold text-teal-500">track your webtoons</h1>

    <h2 class="text-2xl">add a webtoon</h2>

    <form action="/addWebtoon" method="POST">
        <input type="text" name="webtoonTitle" placeholder="webtoon name" id="">
        <input type="text" name="currentChapter" placeholder="current chapter" id="">
        <input type="text" name="source" placeholder="source" id="">
        <input type="submit" value="Submit">
    </form>

    <h2>Currently Reading</h2>
    <ul class="webtoons">
        <% for(let i=0; i < info.length; i++) {%>
            <li class="webtoon">
                <span><%= info[i].webtoonTitle %></span>
                <span><%= info[i].currentChapter %></span>
                <span><%= info[i].source %></span>
                <button class="btn">Add chapter</button>
                <button class="delete">Delete</button>
            </li>
        <% } %>
        </ul>

        <script src="js/index.js"></script>
</body>
</html>