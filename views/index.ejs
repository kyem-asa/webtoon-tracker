<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>webtoon tracker</title>

    <link href="css/output.css" rel="stylesheet">

</head>
<body class="m-8 flex items-center justify-center h-screen">
    <div class="flex flex-col" >
        <h1 class="text-7xl font-bold my-8">(っ◔◡◔)っ ♥ Track Your Webtoons ♥</h1>
        <h2 class="text-4xl">Add a webtoon</h2>
        <div class="flex flex-col">
            <form action="/addWebtoon" method="POST" class="">
                <input type="text" name="webtoonTitle" placeholder="Webtoon name" id="" class="input input-xs input-bordered input-primary w-full max-w-xs my-4">
                <input type="text" name="currentChapter" placeholder="Current chapter" id="" class="input input-xs input-bordered input-primary w-full max-w-xs my-4">
                <input type="text" name="source" placeholder="Source" id="" class="input input-xs input-bordered input-primary w-full max-w-xs my-4">
                <div><input type="submit" value="Add webtoon" class="btn btn-xs"></div>
            </form>
        </div>
        <h2 class="text-2xl my-4">Currently Reading</h2>
        <ul class="webtoons">
            <% for(let i=0; i < info.length; i++) {%>
                <li class="webtoon my-4">
                    <span class="span mr-4"><%= info[i].webtoonTitle %></span>
                    <span class="span mr-4"><%= info[i].currentChapter %></span>
                    <span class="span mr-4"><%= info[i].source %></span>
                    <button class="btn btn-xs add">Add chapter</button>
                    <button type="button" class="delete btn btn-xs">Delete</button>
                </li>
            <% } %>
            </ul>
    </div>

        <script src="js/index.js"></script>
</body>
</html>